# vim:set ft=tmux:
# Requires https://github.com/vascomfnunes/tmux-clima
%hidden MODULE_NAME="clima"

set -ogq "@sonokai_${MODULE_NAME}_icon" " "
set -ogqF "@sonokai_${MODULE_NAME}_color" "#{E:@snk_yellow}"
set -ogq "@sonokai_${MODULE_NAME}_text" " #{l:#{clima}}"

source -F "#{d:current_file}/../utils/status_module.conf"
